<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <parent>
        <artifactId>gephi-plugin-parent</artifactId>
        <groupId>org.gephi</groupId>
        <version>$gephi_version</version>
    </parent>

    <groupId>$org_id</groupId>
    <artifactId>$artifact_id</artifactId>
    <version>$version</version>
    <packaging>nbm</packaging>

    <name>$branding_name</name>

    <dependencies>
        <!-- Insert dependencies here -->

        <!-- Generic SPI-contract tests (e.g. layout builders) run automatically against
             this plugin's implementations, see https://github.com/gephi/gephi-plugins-tests -->
        <dependency>
            <groupId>org.gephi</groupId>
            <artifactId>gephi-plugins-tests</artifactId>
            <version>0.11.3-SNAPSHOT</version>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.netbeans.utilities</groupId>
                <artifactId>nbm-maven-plugin</artifactId>
                <configuration>
#if( $license_name )
                    <licenseName>$license_name</licenseName>
#end
#if( $license_file )
                    <licenseFile>$license_file</licenseFile>
#end
                    <author>$author_name</author>
#if( $homepage_url )
                    <homePageUrl>$homepage_url</homePageUrl>
#end
                    <publicPackages>
                        <!-- Insert public packages -->
                    </publicPackages>
                </configuration>
            </plugin>
#if( $author_email || $author_url || $sourcecode_url )
            <plugin>
                <groupId>org.gephi</groupId>
                <artifactId>gephi-maven-plugin</artifactId>
                <version>$plugin_version</version>
                <configuration>
#if( $author_email )
                    <authorEmail>$author_email</authorEmail>
#end
#if( $author_url )
                    <authorUrl>$author_url</authorUrl>
#end
#if( $sourcecode_url )
                    <sourceCodeUrl>$sourcecode_url</sourceCodeUrl>
#end
                </configuration>
            </plugin>
#end

            <!-- Also run the "*IT" generic contract tests brought in by gephi-plugins-tests,
                 in addition to the default "*Test" pattern -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-plugin</artifactId>
                <version>3.6.0</version>
                <configuration>
                    <includes>
                        <include>**/Test*.java</include>
                        <include>**/*Test.java</include>
                        <include>**/*Tests.java</include>
                        <include>**/*TestCase.java</include>
                        <include>**/*IT.java</include>
                    </includes>
                </configuration>
            </plugin>

            <!-- gephi-plugins-tests ships its contract tests as regular compiled classes in
                 its jar rather than a test-jar, so unpack them into test-classes for Surefire
                 to pick up -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-dependency-plugin</artifactId>
                <version>3.11.0</version>
                <executions>
                    <execution>
                        <id>unpack-gephi-plugins-tests</id>
                        <phase>process-test-classes</phase>
                        <goals>
                            <goal>unpack-dependencies</goal>
                        </goals>
                        <configuration>
                            <includeArtifactIds>gephi-plugins-tests</includeArtifactIds>
                            <includeScope>test</includeScope>
                            <outputDirectory>${project.build.testOutputDirectory}</outputDirectory>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>

    <!-- Snapshot Repositories (only needed if developing against a SNAPSHOT version,
         e.g. of gephi-maven-plugin or gephi-plugins-tests) -->
    <repositories>
        <repository>
            <id>central-portal-snapshots</id>
            <name>Central Portal Snapshots</name>
            <url>https://central.sonatype.com/repository/maven-snapshots/</url>
            <releases>
                <enabled>false</enabled>
            </releases>
            <snapshots>
                <enabled>true</enabled>
            </snapshots>
        </repository>
    </repositories>
</project>


